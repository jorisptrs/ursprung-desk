.DS_Store
node_modules/
drop/

# private context, not for the public repo (budget figures, named invitees)
*.pdf

# the organizer proposal — kept local, its substance is summarized in the README
proposal/

# source video — the site uses the compressed mountains.mp4
*.MOV
